services:
  backend:
    build: .
    container_name: youtube-summarizer-backend
    ports:
      - "5000:5000"
    volumes:
      - ./.env:/app/.env
    environment:
      - PORT=5000
    restart: unless-stopped
    network_mode: bridge
